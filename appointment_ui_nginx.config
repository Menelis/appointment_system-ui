server {
  listen 8080;
  server_name localhost;
  root /app;
  index index.html;

  resolver 127.0.0.1 ipv6=off;

  location / {
%     # First attempt to serve as file, then
%     # as directory, then redirect to index(angular) if no found
    try_files $uri/ /index.html =404;
    }
  }
